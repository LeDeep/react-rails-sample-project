var Header = require('./header');

var Contacts = React.createClass({
    render: function() {
        return (
            <div>
              <Header />
            </div>
        );
    }
});
module.exports = Contacts;
